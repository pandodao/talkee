app_name: "talkee-test"
db:
  driver: "postgres"
  datasource: "user=foobar dbname=talkee host=localhost password=foobar sslmode=disable"

auth:
  jwt_secret: "112233"
  mixin_client_secret: ".."

mixpay:
  payee_id: ""
  callback_url: "https://"
